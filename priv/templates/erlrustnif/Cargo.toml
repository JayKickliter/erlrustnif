[package]
build   = "native/build.rs"
edition = "2021"
name    = "{{name}}-project"
version = "0.1.0"

[lib]
crate-type = ["dylib"]
name       = "{{name}}_native"
path       = "native/lib.rs"

[dependencies]
rustler = "0.36"
